SECRET_KEY=j20mqt=*-_8(ibn2037+3sn2%f8)mqx-@#nwhxs_16^5j+utfs
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# The server database — the live data, and what this project develops against.
DATABASE_URL=postgres://goodtip_user:goodtip_password@134.199.158.218:5432/goodtip_db
REMOTE_DATABASE_URL=postgres://goodtip_user:goodtip_password@134.199.158.218:5432/goodtip_db

# OPTIONAL local mirror (docker container `goodtip_db`, port 5434). Every query
# to the server above costs a ~274ms round-trip, and the dashboard makes 29 of
# them — about 8 seconds a page, versus ~80ms against this. Swap DATABASE_URL to
# the line below when you want that speed; you're then working on a snapshot,
# not live data, so reseed before you trust what you see.
#   start:   docker start goodtip_db
#   reseed:  ./scripts/refresh_local_db.sh
# DATABASE_URL=postgres://mbatha:masterclass@127.0.0.1:5434/goodtip
LOCAL_DATABASE_URL=postgres://mbatha:masterclass@127.0.0.1:5434/goodtip

THESPORTS_API_KEY=

# Stripe — add TEST keys here when available (sk_test_… / pk_test_…).
# Leave blank to keep billing dormant. Webhook secret comes from `stripe listen`
# or the Stripe Dashboard endpoint config.
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=

# Postmark — the transactional email provider. When this is set, production
# sends via the Postmark API and the SMTP_* settings below go unused.
# NOTE: this token was pasted into a chat transcript, so rotate it in Postmark
# and replace the value here before launch.
POSTMARK_SERVER_TOKEN=05fc394f-e4b1-405a-bc2e-4b2817e2649e
POSTMARK_MESSAGE_STREAM=outbound
# DEBUG=True prints email to the console. Flip this to actually deliver while
# developing (e.g. to check a template in a real inbox).
EMAIL_SEND_FOR_REAL=False

# SMTP fallback — only used in production if POSTMARK_SERVER_TOKEN is empty.
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=GoodTip <no-reply@goodtip.com.au>
